import 'package:flutter/material.dart';

class AngkutinPage extends StatelessWidget {
  const AngkutinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.white,
      ),
    );
  }
}